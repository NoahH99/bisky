apiVersion: 1

datasources:
  # The uid is pinned so provisioned dashboards can reference it directly
  # instead of carrying whatever random uid an export happened to contain.
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
